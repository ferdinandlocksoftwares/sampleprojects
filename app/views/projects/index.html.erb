<h1>Projects#index</h1>
<p>Find me in app/views/projects/index.html.erb</p>
<% @projects.each do | project | %>
<table>
    <tr>
        <td><%= project.title %></td>
        <td><%= project.description %></td>
        <td><%= project.destroy %></td>
    </tr>
</table>
<% end %>